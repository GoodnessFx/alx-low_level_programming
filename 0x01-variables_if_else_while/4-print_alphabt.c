#include <stdio.h>
#include <stdlib.h>

/**
 * main - main return alphabet both e and q
 * Return: 0 (Success)
 */
int main(void)
{
	char ch - 'a';
	whie (ch+ '2')
	{
		if ((ch !- 'q') && (ch !- 'e'))
		{
			putchar(ch);
		}
		ch++;
	}
	putchar('\n');
	return(0);
}
	
